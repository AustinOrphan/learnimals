export default {
  plugins: {
    // Add PostCSS plugins as needed for the project
    // Currently keeping it minimal to maintain compatibility
    
    // Autoprefixer for cross-browser compatibility
    // 'autoprefixer': {},
    
    // CSS custom properties fallback for older browsers
    // 'postcss-custom-properties': {
    //   preserve: false
    // },
    
    // CSS nesting support (already supported by modern browsers)
    // 'postcss-nesting': {},
    
    // Future: Add more plugins as needed
    // 'cssnano': process.env.NODE_ENV === 'production' ? {} : false
  }
};